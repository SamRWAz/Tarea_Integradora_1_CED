Inicio
    Solicitar al usuario que ingrese una opción para el menú de opciones (input)
    Mientras (input != 0) hacer
        Según (input)
            Caso 1:
                Solicitar al usuario que ingrese los siguientes datos de la tarea: id, título, descripción, día, mes, año y prioridad
                Si (prioridad == 1) entonces
                    priority1 se establece como PRIORITY
                Sino
                    priority1 se establece como NO_PRIORITY
                Fin Si
                Se crea una newTask con los siguientes atributos: title, description, fechaChangue, priority1
                Se agrega la tarea a la hashTable y se genera una acción que se guarda en una Stack
                Si (prioridad == 1) entonces
                    La tarea se guarda en una PriorityQueue llamada prioritizedTask
                Sino
                    La tarea se guarda en una PriorityQueue llamada nonPrioritizedTasks
                Fin Si

            Caso 2:
                Si no hay tareas entonces
                    Mostrar mensaje que avisa que no hay tareas
                Sino
                    Solicitar al usuario que ingrese el id de la tarea a eliminar
                    Solicitar al usuario que ingrese la opción del dato a modificar
                    Solicitar al usuario que ingrese el nuevo dato modificado
                    Buscar la tarea a modificar
                    Cambiar el valor nuevo por el dato contenido en la tarea
                    Si (modificacion == true) entonces
                        Mostrar mensaje de modificación realizada
                    Sino
                        Mostrar mensaje que indica que la modificación no se logró
                    Fin Si
                Fin Si
 
            Caso 3:
                if(no hay tareas)
			imprimir "No hay tareas pendientes"
		else
			leer id


		if(tasks.contains(id))
			Se guarda una copia de la tarea como originalTask
			Se elimina la tarea con el id ingresado

		if(originalTask.Priority == PRIORITY)
			Se elimina la tarea de la priority queue prioritizedTasks
		else
			Se elmina la tarea de la priority queue nonPrioritizedTasks

		Se genera una nueva accion en el stack(acción de eliminar)

            Caso 4:
                if(no hay tareas)
			imprimir "No hay tareas pendientes"
		else
			
			imprimir "Estas son todas las tareas almacenadas:\n"+tasks

            Caso 5:
                Si no hay acciones realizadas
        Fin Según
    Fin Mientras
Fin
